const path = require('path');
const fs=require('fs');
const config = ('project.config.js');

var pathsToKeepArr = config.KEEP_FILES;
var filesToMove = config.MOVE_FILES;

if (process.env.BUILD_MODE === 'production'){
    rm
}

if (process.env.BUILD_MODE === 'production'){
    filesToMove.concat(config.MOVE_FILES_DEV);
}

if ()

